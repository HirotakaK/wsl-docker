# winget(マイクロソフト製のWindowsパッケージ管理ソフト)

## Claude Code

### Google Cloud SDK 認証

初回操作が必要

``` 
gcloud auth login
gcloud auth application-default login
```

### 環境変数設定

環境変数設定済なので考慮不要

```
export CLAUDE_CODE_USE_VERTEX=1
export CLOUD_ML_REGION=global
export ANTHROPIC_VERTEX_PROJECT_ID=trading-3986
```
